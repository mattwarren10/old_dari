

<%= link_to("<< Back to List", words_index_path, :class => 'back-link') %>

<div class="subjects edit">
  <h2>Update Subject</h2>

  <%= form_for(@word) do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>

    <div class="form-buttons">
      <%= f.submit("Update Word") %>
    </div>

  <% end %>
</div>